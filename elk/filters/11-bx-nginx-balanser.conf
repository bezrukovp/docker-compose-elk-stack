filter {
  if [type] == "bx-nginx-access-balancer" {
    grok {
      match => { "message" => "%{BXNGINXACCESSBALANCER}" }
    }
    date {
      match => ["timestamp", "ISO8601"]
      timezone => "UTC"
      target => "@timestamp"
    }
  }
}
